/* =========================================================
 * loginInicio.js
 * http://lls.net.br/
 * ========================================================= */

function loginInicio() {
	
	$('meta[name=description]')
		.attr('content', nomeProjeto());
	
	container();
	
}

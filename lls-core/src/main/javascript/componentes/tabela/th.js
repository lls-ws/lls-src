/* =========================================================
 * th.js
 * http://lls.net.br/
 * ========================================================= */

function th(classes) {
	
	var $th = $('<th />').addClass(classes);
	
	return $th;
	
}

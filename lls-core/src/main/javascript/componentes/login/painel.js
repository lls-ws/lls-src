/* =========================================================
 * painel.js
 * http://lls.net.br/
 * ========================================================= */

function painel(classes) {
	
	var $painel = $("<div/>").attr({id: "painel"}).addClass(classes);
	
	$('.container').append($painel);
	
}

/* =========================================================
 * span.js
 * http://lls.net.br/
 * ========================================================= */

function span(classes) {
	
	var $span = $("<span/>").addClass(classes);
	
	return $span;
	
}

/* =========================================================
 * title.js
 * http://lls.net.br/
 * ========================================================= */

function title() {
	
	var $title = $('<title />').append(nomeProjeto());
	
	return $title;
	
}

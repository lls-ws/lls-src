/* =========================================================
 * login.js
 * http://lls.net.br/
 * ========================================================= */

function login(tipo) {
	
	var dados = {
		tipo: tipo,
		formulario: 'Login'
	}
	
	core(dados);
	
}
